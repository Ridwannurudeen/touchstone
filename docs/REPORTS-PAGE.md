For the merge, add `subprocess.run([sys.executable, str(ROOT / "scripts" / "build_reports.py")], check=True)` immediately before `sources = sorted(PAGES.rglob("*.html"))` in `scripts/build_site.py`, then add `<a href="/reports">Reports</a>` immediately after the Assets link in `site2/_partials/header.html`; the generator refreshes `site2/_pages/reports.html`, which the existing page loop renders to `/reports` with the shared header and footer.
